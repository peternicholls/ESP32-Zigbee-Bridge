# ESP32-C6 Zigbee Bridge - Visual Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ESP32-C6 Hardware                                │
│                    (Wi-Fi + 802.15.4 Radio)                             │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────────┐
│                         ESP-IDF / FreeRTOS                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
│  │   Wi-Fi Stack   │  │  Zigbee Stack   │  │   Hardware HAL  │        │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘        │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────────┐
│                    Tiny OS Kernel (FreeRTOS Task)                        │
│  ┌──────────┐ ┌──────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌───────┐ │
│  │  Fibres  │ │Event Bus │ │Timers  │ │Logging │ │Persist │ │ Shell │ │
│  │Scheduler │ │ Pub/Sub  │ │HW/SW   │ │Enhanced│ │  NVS   │ │  CLI  │ │
│  └──────────┘ └──────────┘ └────────┘ └────────┘ └────────┘ └───────┘ │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────────┐
│                     Zigbee Canonical Model                               │
│                                                                          │
│  Node (0x00124B001234ABCD)                                              │
│    ├─ Endpoint 1 (Dimmable Light)                                       │
│    │   ├─ Cluster 0x0006 (On/Off)                                       │
│    │   │   └─ Attribute 0x0000 (OnOff): bool = true                     │
│    │   ├─ Cluster 0x0008 (Level Control)                                │
│    │   │   └─ Attribute 0x0000 (CurrentLevel): uint8 = 255              │
│    │   └─ Cluster 0x0300 (Color Control)                                │
│    │       └─ Attribute 0x0007 (ColorTemp): uint16 = 370                │
│    └─ Endpoint 2 (Temperature Sensor)                                   │
│        └─ Cluster 0x0402 (Temperature Measurement)                      │
│            └─ Attribute 0x0000 (MeasuredValue): int16 = 2250            │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────────┐
│                        Capability Layer                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐      │
│  │ Light Capability │  │Switch Capability │  │Sensor Capability │      │
│  │                  │  │                  │  │                  │      │
│  │ • light.on()     │  │ • switch.on()    │  │ • get_temp()     │      │
│  │ • light.off()    │  │ • switch.off()   │  │ • get_humidity() │      │
│  │ • set_bright()   │  │ • switch.toggle()│  │ • get_occupancy()│      │
│  │ • set_color()    │  │ • get_state()    │  │ • get_battery()  │      │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘      │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────────┐
│                     Northbound Interfaces                                │
│  ┌─────────────────────────────────────────────────────────────┐        │
│  │                      MQTT Bridge                              │        │
│  │                                                               │        │
│  │  Subscribe: zigbee2mqtt/+/set                               │        │
│  │  Publish:   zigbee2mqtt/<device_addr>/<capability>/state    │        │
│  │                                                               │        │
│  │  {"state":"ON","brightness":255,"color_temp":370}           │        │
│  └─────────────────────────────────────────────────────────────┘        │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────┐        │
│  │              Future: Matter Bridge                           │        │
│  │              Future: HomeKit Bridge                          │        │
│  └─────────────────────────────────────────────────────────────┘        │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
            ┌───────▼────────┐    ┌──────▼──────┐
            │  MQTT Broker   │    │ Home        │
            │  (Cloud/Local) │    │ Automation  │
            └────────────────┘    └─────────────┘
```

## Data Flow Examples

### 1. Device State Change Flow

```
┌─────────────┐
│Zigbee Device│ Physical button pressed
│  (Light)    │
└──────┬──────┘
       │ Zigbee On/Off command
       ▼
┌─────────────────┐
│ Zigbee Stack    │ Receives attribute report
│ (ESP-IDF)       │
└──────┬──────────┘
       │ Update attribute
       ▼
┌─────────────────┐
│ Zigbee Model    │ Attribute.value = true
│   Attribute     │
└──────┬──────────┘
       │ Publish event
       ▼
┌─────────────────┐
│   Event Bus     │ EVENT_ZIGBEE_ATTRIBUTE_CHANGED
└──────┬──────────┘
       │ Notify subscribers
       ▼
┌─────────────────┐
│  MQTT Bridge    │ on_attribute_changed()
└──────┬──────────┘
       │ Publish MQTT
       ▼
┌─────────────────┐
│  MQTT Broker    │ zigbee2mqtt/0x1234/light/state
└─────────────────┘  {"state":"ON"}
```

### 2. MQTT Command Flow

```
┌─────────────────┐
│ Home Assistant  │ User turns on light
└──────┬──────────┘
       │ MQTT publish
       ▼
┌─────────────────┐
│  MQTT Broker    │ zigbee2mqtt/0x1234/set
└──────┬──────────┘  {"state":"ON"}
       │
       ▼
┌─────────────────┐
│  MQTT Bridge    │ on_mqtt_message()
└──────┬──────────┘
       │ Parse command
       ▼
┌─────────────────┐
│ Light Capability│ light_on(device)
└──────┬──────────┘
       │ Map to cluster
       ▼
┌─────────────────┐
│ Zigbee Cluster  │ Send On command
│   (On/Off)      │
└──────┬──────────┘
       │ Zigbee protocol
       ▼
┌─────────────────┐
│Zigbee Device    │ Light turns on
│  (Light)        │
└─────────────────┘
```

## Component Interactions

```
┌────────────────────────────────────────────────────────────┐
│                     Application Layer                       │
├────────────────────────────────────────────────────────────┤
│  Shell Commands ──┐                                         │
│  User Scripts   ──┼─► Capability Layer ──► Device Control  │
│  Automation     ──┘                                         │
└────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────┐
│                      Event Bus Core                         │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Zigbee   │─►│Event Bus │◄─│  MQTT    │◄─│  Shell   │   │
│  │  Model   │  │          │  │  Bridge  │  │          │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│       ▲             │              │              │         │
│       │             ▼              ▼              ▼         │
│  ┌────────────────────────────────────────────────────┐    │
│  │        Tiny OS Kernel Services                     │    │
│  │  • Persistence  • Timers  • Logging  • Fibres      │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────┘
```

## File Organization Map

```
ESP32-Zigbee-Bridge/
│
├── 📄 Configuration & Build
│   ├── CMakeLists.txt          (Top-level project)
│   ├── sdkconfig.defaults      (ESP32-C6, Zigbee, Wi-Fi)
│   ├── partitions.csv          (NVS, factory, storage)
│   └── .gitignore              (Build artifacts)
│
├── 📚 Documentation
│   ├── README.md               (Quick start, 5K+ words)
│   ├── ARCHITECTURE.md         (Design doc, 10K+ words)
│   └── IMPLEMENTATION_SUMMARY.md (Project summary)
│
├── 🎯 Main Application
│   └── main/
│       ├── CMakeLists.txt
│       └── main.c              (Entry point, initialization)
│
├── 🔧 Components
│   ├── tiny_os/                (Kernel-in-a-task)
│   │   ├── include/            (7 public headers)
│   │   ├── tiny_os.c           (Kernel core)
│   │   ├── fibre.c             (Green threads)
│   │   ├── event_bus.c         (Pub/sub)
│   │   ├── timer.c             (Timers)
│   │   ├── logger.c            (Logging)
│   │   ├── persistence.c       (NVS)
│   │   └── shell.c             (CLI)
│   │
│   ├── zigbee_model/           (Canonical model)
│   │   ├── include/            (5 public headers)
│   │   ├── zigbee_model.c      (Layer init)
│   │   ├── node.c              (Devices)
│   │   ├── endpoint.c          (Sub-devices)
│   │   ├── cluster.c           (Functions)
│   │   └── attribute.c         (Data points)
│   │
│   ├── capability/             (High-level API)
│   │   ├── include/            (4 public headers)
│   │   ├── capability.c        (Layer init)
│   │   ├── light_capability.c  (Lights)
│   │   ├── switch_capability.c (Switches)
│   │   └── sensor_capability.c (Sensors)
│   │
│   └── mqtt_bridge/            (Northbound)
│       ├── include/            (1 public header)
│       └── mqtt_bridge.c       (MQTT client)
│
└── 📖 Examples
    ├── README.md               (Usage guide)
    ├── device_control.c        (Device management)
    ├── fibre_usage.c           (Green threads)
    ├── timer_usage.c           (Timers)
    ├── shell_commands.c        (CLI commands)
    └── mqtt_integration.c      (MQTT patterns)
```

## Technology Stack

```
┌─────────────────────────────────────────────────────┐
│              Application Code                        │
│         C language, ESP-IDF APIs                     │
└─────────────────────────────────────────────────────┘
                       │
┌─────────────────────────────────────────────────────┐
│            ESP-IDF Framework v5.1+                   │
│  • esp_timer   • esp_wifi    • esp_netif             │
│  • nvs_flash   • mqtt_client • esp_event             │
└─────────────────────────────────────────────────────┘
                       │
┌─────────────────────────────────────────────────────┐
│                  FreeRTOS RTOS                       │
│  • Tasks  • Queues  • Semaphores  • Timers          │
└─────────────────────────────────────────────────────┘
                       │
┌─────────────────────────────────────────────────────┐
│               ESP32-C6 Hardware                      │
│  • RISC-V CPU  • Wi-Fi  • 802.15.4  • Flash         │
└─────────────────────────────────────────────────────┘
```
